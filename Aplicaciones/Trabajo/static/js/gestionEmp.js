(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacion");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este empleado?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionFruta");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionDespate");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionLavado");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionCongelado");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionQuebrado");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();

(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionMaquila");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionP");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();
(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionC");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();

(function(){

    const btnEliminacion = document.querySelectorAll(".btnEliminacionCom");

    btnEliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const confirmacion = confirm('¿Seguro de eliminar este Movimiento?');
            if (!confirmacion) {
                e.preventDefault();
            }
        });
    });

})();


